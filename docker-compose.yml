services:
  api:
    build:
      context: .
      dockerfile: services/api/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    env_file:
      - path: /root/.argus-spacetrack.env
        required: false
    restart: unless-stopped

  web:
    build:
      context: apps/web
      dockerfile: Dockerfile
    ports:
      - "80:80"
      - "8080:80"
    depends_on:
      - api
    restart: unless-stopped
